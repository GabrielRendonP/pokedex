<%= form_with(model: poke_ability) do |form| %>
  <% if poke_ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poke_ability.errors.count, "error") %> prohibited this poke_ability from being saved:</h2>

      <ul>
        <% poke_ability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
